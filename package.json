{
  "name": "gym-membership-logs",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run web\" \"npm run fingerprint:bridge\"",
    "web": "live-server frontend --open=index.html",
    "fingerprint:bridge": "node backend/src/modules/fingerprint-detection/bridge.js"
  },
  "dependencies": {
    "live-server": "^1.2.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
